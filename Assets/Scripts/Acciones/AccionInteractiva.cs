using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AccionInteractiva : Accion
{

    private Personaje p;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void realizar(Personaje p1, Computadora comp, Personaje p2){
        //realizar accion mepa que no va
    }
}
